#include <stdio.h>

/**
 * main - prints all single digit numbers starting from zero
 *
 * Return: returns zero
 */
int main(void)
{
	int num = 0;

	for (num = 0; num < 10; num++)
	{
		printf("%d\n", num);
	}
	return (0);
}
