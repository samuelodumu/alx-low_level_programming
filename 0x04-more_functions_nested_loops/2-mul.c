#include "main.h"

/**
 * mul - multiplies two numbers
 * @a: the first number
 * @b: the second number
 *
 * Return: 1 if c is a digit, 0 otherwise
 */

int mul(int a, int b)
{
	int product;

	product = a * b;
	_putchar(product + '0');

	return (0);
}
